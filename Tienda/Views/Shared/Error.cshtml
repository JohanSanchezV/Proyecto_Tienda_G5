@{
    ViewBag.Title = "Error";
}

<div class="section auth-section">
    <div class="container">
        <div class="auth-card text-center">
            <i class="fa fa-exclamation-triangle profile-icon"></i>
            <h2>Ocurrió un error</h2>
            <p>No fue posible completar la operación.</p>
            <a class="primary-btn" href="@Url.Action("Index", "Home")">
                Volver al inicio
            </a>
        </div>
    </div>
</div>
